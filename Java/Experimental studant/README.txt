Explanation of the first part:
At first, two different solutions should be found for the problem that will be explained later. The first solution is recursive and the second solution is non-recursive. Write a program for each of the solutions and upload it to Quora in the relevant part of the question. The pieces of code sent are judged by test bags.
Note that you should also calculate the theoretical time complexity of your programs along with the complete solution and write it down in the relevant document.
Number decoding problem:
Sarah wants to use special letters instead of normal words to communicate confidential issues with her friend, the meaning of these letters is determined based on an agreement between Sarah and her friend. After a lot of trial and error, Sarah realizes that writing the same letters one after the other is very time-consuming; So, he decides to write a program to produce them, which, having the number of relevant threads, will print it in the specified number, but he has no programming knowledge. So
Help Sarah write her program.
At first, your program receives arrays of numbers and letters as input and prints them in the desired number as output. The format of the inputs is (k) string, where the string inside the "" sign must be repeated exactly k times, where k must be a positive number and greater than zero. Note that the strings do not contain anything other than letters.
English is not small.
For example, the input a)2(bv)cb(3) has the output aaabvbvcb. The number before the sign “ ” specifies the number of letters/character strings. If there is no number before the letter/character string, that is The number of letters/character strings is not more than one.
Part II:
In this problem, you should implement linear search, binary search and ternary search algorithms. Then, compare their execution time with each other in the way that we will discuss in the following.
Comparison of algorithms
This problem will have no input from the user, and its steps will be done by generating random arrays and random values for its searches. After implementing these algorithms, you should generate random arrays with lengths of 10 to 10,000 in different stages and then count how many operations each of these algorithms perform to search for element x in that random array. Then, with the help of algorithm execution order and the length of the obtained arrays, draw the operation-elements diagram of each algorithm separately. (For this, you can enter your data in Excel or other similar software and then convert it into a chart, or by using libraries and
Draw the coding diagram.)
All the required building data of the project must be implemented by you